name: Deploy VitePress to GitHub Pages and Windows IIS

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build with VitePress
        run: npm run docs:build

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .vitepress/dist

      - name: Prepare IIS Artifact
        run: |
          mkdir -p dist-iis
          cp -r .vitepress/dist/* dist-iis/
          tar -czf dist-iis.tar.gz dist-iis/

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist-iis
          path: dist-iis.tar.gz

  deploy-to-pages:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  deploy-to-iis:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: dist-iis

      - name: Test SSH Connection
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            echo "SSH connection successful!"

      - name: Deploy Files
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "dist-iis.tar.gz"
          target: "C:\\Temp\\dist-iis.tar.gz"
          strip_components: 1

      - name: Extract and Configure
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            # 创建目标目录
            mkdir C:\web\vitepress-app -Force
            # 解压文件
            tar -xzf C:\Temp\dist-iis.tar.gz -C C:\web\vitepress-app
            del C:\Temp\dist-iis.tar.gz
            # 配置 IIS
            Import-Module WebAdministration
            if (!(Test-Path IIS:\Sites\VitePressSite)) {
              New-WebSite -Name "VitePressSite" -Port 80 -PhysicalPath "C:\web\vitepress-app" -ApplicationPool "DefaultAppPool"
              Add-WebConfiguration -Filter "/system.webServer/defaultDocument/files" -Value @{value='index.html'}
            }
            iisreset /restart